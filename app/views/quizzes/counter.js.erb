// was tring to crqeate ajax for quiz - but if we want to implemant it, well need to create the entire controller from the begining!
// function nextQuestionForm(innerHTML) {
//   const newQuizForm = document.querySelector('.quiz_thing');
//   newQuizForm.innerHTML = innerHTML;
//   console.log('I am working!!!!');
// }

// nextQuestionForm('<%= j redirect_to questions_quiz_path(@quiz) %>');
