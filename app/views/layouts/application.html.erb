<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Argo</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  </head>
  <body style="background-image: url('<%= asset_url("background_of_app.png") %>')">
    <div class="page-render <%= "d-flex justify-content-between flex-column" if current_page?('/quizzes/starter/') %>">
      <%= render 'shared/flashes' %>
      <%= yield %>
    </div>
    <%= render 'shared/navbar' unless current_page?(root_path) %>
    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
