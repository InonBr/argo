<div class="user-language-show">
  <div class="white-container d-flex p-4 my-5">
    <div class="start_practice_top">
      <h1 class="thirty-two-font color-grey">Start Practicing</h1>
    </div>
   <%= image_tag "#{@user_language.language.name.downcase}.png", alt: "#{@user_language.language.name}", class: 'user-language-img' unless @user_language.nil?  %>


  </div>

  <div class="pt-2">
    <% if @random_word_in_user_language %>
      <div class="white-container mt-5 text-center pill-button">
        <%= link_to 'Flashcard', new_word_user_word_path(@random_word_in_user_language), class: "link" %>
      </div>
    <% else %>
      <div class="white-container mt-5 text-center">
        <%= link_to 'Flashcard', "#", class: "link disabled-link" %>
      </div>
    <% end %>
          <!--<div class= "quiz">-->

    <div class="white-container mt-4 text-center pill-button">
      <% if @can_take_quiz %>
        <%= link_to 'Quiz', starter_quizzes_path,  class: "link"%>
      <% else %>
        <div class="link">Quiz 🔒</div>
      <% end %>

    </div>
  </div>
</div>
